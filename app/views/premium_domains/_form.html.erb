<%= form_with(model: premium_domain, local: true) do |form| %>
  <% if premium_domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(premium_domain.errors.count, "error") %> prohibited this premium_domain from being saved:</h2>

      <ul>
      <% premium_domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :offre %>
    <%= form.text_area :offre %>
  </div>

  <div class="field">
    <%= form.label :date_debut %>
    <%= form.date_select :date_debut %>
  </div>

  <div class="field">
    <%= form.label :date_fin %>
    <%= form.date_select :date_fin %>
  </div>

  <div class="field">
    <%= form.label :categorie_id %>
    <%= form.text_field :categorie_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
